const defaultString = "Requests";

export const enum MetadataKeysRequests {
  CONTROLLERS = `${defaultString}:controllers`,
  ROUTES = `${defaultString}:routes`,
  REQ = `${defaultString}:requests`,
  RES = `${defaultString}:response`,
  NEXT = `${defaultString}:nextFunction`,
}
